[
  { "name": "price rejects unknown ticker with a correction", "steps": [{ "send": { "text": "/price DOGE" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Couldn't find DOGE. Use a common ticker such as BTC, ETH, TON, SOL, USDT, or XRP." } }] }] },
  { "name": "price returns a current quote", "steps": [{ "send": { "text": "/price BTC" }, "expect": [{ "method": "sendMessage" }] }] }
]
